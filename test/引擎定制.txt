. PageView添加velocity tracker
. PageView使用datasource动态创建page item, 基本重写了PageView, 注释掉WidgetPropertiesReader0300::widgetFromJsonDictionary中创建pageitem的代码, 不再支持从cocostudio中创建pageitem
. ListView的refreshView改成public
. CCTableView让dequeueCell支持cell id
. WidgetPropertiesReader0300::widgetFromJsonDictionary, 读取到Node时创建Panel
. WidgetPropertiesReader0300::createWidget, 检查文件版本以便支持Mac版cocostudio 1.0.0, 通过isMac标志做处理
. Widget::removeNodeByTag, 删掉log, 太烦人
. Label构造函数, _fontSize缺省改为20, 因为mac版cocostudio默认字体大小为20, 当为20时, 不会序列化到json中
. LabelBMFont::setFntFile, fix memory leak
. WidgetPropertiesReader0300::setPropsForLabelFromJsonDictionary, mac版cocostudio的label如果不设置文字, 不会保存为空字符串, 而是返回NULL, 所以追加空指针检查以保证mac版兼容性
. 把UIWidget的pushDownEvent, moveEvent, releaseUpEvent, cancelUpEvent改成public方便模拟事件
. UIListView加了一个getTouchEndPoint方法
. CCTableView保存了点击结束位置
. CCScrollView添加设置min scale和max scale的方法
. CCLabelBMFont增加了setLineKerning, 支持对行距进行紧缩
. CCProtocols.h增加CCMeasurableProtocol, 让LoadingBar, CCProgressTimer实现CCMeasurableProtocol
. CCProgressTo, CCProgressFromTo对CCMeasurableProtocol进行操作
. CCLayer增加is/setSwallowTouch, 从而可以控制CCScrollView是否吞掉touch